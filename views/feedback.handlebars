<!DOCTYPE html>
<html lang="en">
  <head>
    {{> head}}
    <link rel="stylesheet" type="text/css" href="/css/profiles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
  	<div class="container">
      <div class="form-group">
        <a href = '/intervieweeProfile'><button type="submit" class="btn btn-default">Back To My Profile</button>
      </div>
      {{> logout}}
      <form role="form" id = "edit-profile-form" method="get" action="/intervieweeProfile">
        <p for="feedback">Share your feedback for interview with 
          {{ match }} </p>
        <textarea class="form-control" rows="3" name="feedback">{{feedback}}
        </textarea>
        <br>
        <br>
        <button id="submit" type="submit" class="btn btn-primary match btn-lg active">Save</button>
      </form>
    </div>
 </body>
</html>